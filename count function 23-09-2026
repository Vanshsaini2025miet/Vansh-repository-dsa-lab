#include <stdio.h>
#include <stdlib.h>

struct Node {
    int data;
    struct Node *next;
};
int countlinkedlist(struct Node *head) {
    int count = 0;
    struct Node *t = head;
    while (t != NULL) {
        count++;
        t = t->next; 
    }
    
    return count;
}
